Create table categories_1(
		categoryid int auto_increment primary key,
        category varchar(255) not null
        );
        
insert into categories_1(categoryid, category)
		values(5, "Soundbox" );
	
select * from categories_1;

insert into categories_1(categoryid, category)
		values(5, "Soundbox" );
        
        select * from categories_1;